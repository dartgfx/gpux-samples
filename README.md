# gpux-samples

Sample apps for [gpux](https://github.com/dartgfx/gpux) packages.

## PBR glTF Viewer

Loads a glTF model with PBR shading using `flutter_gpux` and `gltfx`.

```
flutter run -d macos
```
